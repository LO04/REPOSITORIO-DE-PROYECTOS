@model IEnumerable<EventosUPC.Models.Evento_Equipo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    <button class="btn btn-lg" type="submit">@Html.ActionLink("Agregar Equipo", "Create")</button>
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Equipo.nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Evento.nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Equipo.nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Evento.nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_evento_equipo }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_evento_equipo }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_evento_equipo })
        </td>
    </tr>
}

</table>
